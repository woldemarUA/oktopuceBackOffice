import React from 'react';

const InterventionsQuestionsComponent = ({
  onChange,
  record,
  property,
  resource,
}) => {
  console.log(record, property, resource);
  return <div>InterventionsQuestionsComponent</div>;
};

export default InterventionsQuestionsComponent;
